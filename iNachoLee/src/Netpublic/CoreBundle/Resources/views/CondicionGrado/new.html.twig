<h1>CondicionGrado creation</h1>

<form action="{{ path('condiciongrado_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('condiciongrado') }}">
            Back to the list
        </a>
    </li>
</ul>
