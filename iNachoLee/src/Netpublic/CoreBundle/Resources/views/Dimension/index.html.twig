<table class="table">
    <thead class="header">
        <tr>
            <th class="blue header">Id</th>
            <th class="yellow header">Nombre</th>                                  
            <th class="red header">Acciones</th>
        </tr>
    </thead>
    <tbody>

        <div style="background-color:whitesmoke" class="well">
                                                           
            <div style="float: right;"><a href="#" onclick="newinfoAcademicaDefectoAno({{tipo}});" >
            <i class="icon-file"></i> Nuevo.
                </a></div>
        </div>        
            
{% for entity in entities %}
        <tr>
            <td><a onclick="showinfoAcademicaDefectoAno('{{entity.tipo}}','{{entity.id}}')">{{ entity.id }}</a></td>
            <td>{{ entity |raw}} ({{entity.padre}} {{entity.asignatura}})</td>                        
            
            <td>
                <div id="{{entity.id}}" style="display: inline">                    
                    
                    
                        <a href="#" onclick="showinfoAcademicaDefectoAno('{{entity.tipo}}','{{entity.id}}')" >show</a>
                   |
                        <a href="#"   onclick="editarinfoAcademicaDefectoAno('{{entity.tipo}}','{{entity.id}}')" >editar</a>
                   |                  
                        
                    <form style="display: inline"
                          name="form_{{entity.id}}" 
                          id="form_{{entity.id}}">                                                                          
<button style="
        color: rgb(74, 174, 216);
        display: inline;
        background: none;
        border-style:none; " 
        onclick="deleteDimensioninfoAcademicaDefectoAno('{{entity.tipo}}','{{entity.id}}');">
<i class="icon-remove"></i>
                    Eliminar</button>
                    {{ form_widget(delete_form[loop.index0]) }}      
</form>

                    </div>                                        
                
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
<a href="#" onclick="newinfoAcademicaDefectoAno({{tipo}});" >Nuevo</a>               
