<h1>ValorVariable list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nucleo_privado</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('valorvariable_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.valor }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('valorvariable_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('valorvariable_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('valorvariable_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
