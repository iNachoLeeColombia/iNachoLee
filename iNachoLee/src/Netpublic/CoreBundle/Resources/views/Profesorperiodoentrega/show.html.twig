<h1>Profesorperiodoentrega</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Fecha_inicio</th>
            <td>{{ entity.fechainicio|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Fecha_final</th>
            <td>{{ entity.fechafinal|date('Y-m-d H:i:s') }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('Profesorperiodoentrega') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('profesorperiodoentrega_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('profesorperiodoentrega_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
