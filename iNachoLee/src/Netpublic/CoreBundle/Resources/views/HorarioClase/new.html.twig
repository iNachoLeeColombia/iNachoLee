{%if es_ajax%}   
            <div class="modal-header">
              <a class="close" data-dismiss="modal">×</a>
              <h3>Horarios de Aula.</h3>
            </div>
            <div class="modal-body"> 
                <div style="width: 100px;float: left">
                {{ form_widget(form) }}
               </div>    
                    
 
<div style="clear: both"></div>                       
            </div>
            <div class="modal-footer">
              <a href="#" class="btn" data-dismiss="modal">Finalizar</a>
              <button type="submit" id="btn_gruardar_contrato" class="btn btn-primary" onclick="agregarNuevoHorarioaulaAula();">Crear</button>
            </div>
{%else%}          
<h1>HorarioClase creation</h1>

<form action="{{ path('horarioclase_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('horarioclase') }}">
            Back to the list
        </a>
    </li>
</ul>
{%endif%}